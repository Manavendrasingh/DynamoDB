apiVersion: v1
kind: Service
metadata:
  name: dynamo-db-api
  namespace: dynamo-db-api
spec:
  type: LoadBalancer
  selector:
    app: dynamo-db-api
  ports:
    - name: http
      port: 80
      targetPort: 3000
